<% provide(:title, 'Create a study') %>
<h1>Create a study</h1>
<div class="row">
	<div class="span6 offset3">
		<%= form_for(@study) do |f| %>
			<%= render 'fields', f: f %>
			<%= f.submit "Create study", class: "btn btn-large btn-primary" %>
		<% end %>
	</div>
</div>

